// Функции
function foo(){ // Семантика
  console.log('Hello'); // Тело
}

foo(); // Вызов

function sum(a, b){ // Аргументы
  return a + b; // Использование параметров и возврат из функции
}

var result = sum(5, 2); // Вызов с параметрами и возврат результата

// Вызови sum с 1 параметром и посмотри на результат
// Почему он такой? Попробуй добавить в тело функции вывод значений параметров и 
// посмотреть что передется в параметр, если параметр не передан
